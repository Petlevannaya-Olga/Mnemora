<UserControl
    x:Class="Mnemora.Desktop.Views.Sections.DeleteSectionDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Width="520"
    Background="Transparent"
    SnapsToDevicePixels="True"
    TextOptions.TextFormattingMode="Display"
    TextOptions.TextRenderingMode="ClearType"
    UseLayoutRounding="True">

    <UserControl.InputBindings>
        <KeyBinding
            Key="Escape"
            Command="{Binding CancelCommand}" />
    </UserControl.InputBindings>

    <Grid Margin="22">

        <!-- Отдельный слой тени -->

        <Border
            Margin="1"
            Background="{StaticResource Mnemora.Brush.Surface}"
            CornerRadius="18">

            <Border.Effect>
                <DropShadowEffect
                    BlurRadius="26"
                    Color="#061735"
                    Opacity="0.28"
                    ShadowDepth="7" />
            </Border.Effect>

        </Border>

        <!-- Содержимое -->

        <Border
            Padding="30"
            Background="{StaticResource Mnemora.Brush.Surface}"
            BorderBrush="{StaticResource Mnemora.Brush.Border}"
            BorderThickness="1"
            CornerRadius="18">

            <StackPanel>

                <!-- Заголовок -->

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Border
                        Width="48"
                        Height="48"
                        Background="#FFF1F1"
                        CornerRadius="12">

                        <materialDesign:PackIcon
                            Width="27"
                            Height="27"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Foreground="#C62828"
                            Kind="DeleteOutline" />

                    </Border>

                    <StackPanel
                        Grid.Column="1"
                        Margin="15,0"
                        VerticalAlignment="Center">

                        <TextBlock
                            FontSize="21"
                            FontWeight="SemiBold"
                            Foreground="{StaticResource Mnemora.Brush.TextPrimary}">

                            <TextBlock.Style>
                                <Style TargetType="TextBlock">

                                    <Setter
                                        Property="Text"
                                        Value="Удалить раздел?" />

                                    <Style.Triggers>

                                        <DataTrigger
                                            Binding="{Binding HasTopics}"
                                            Value="True">

                                            <Setter
                                                Property="Text"
                                                Value="Раздел нельзя удалить" />

                                        </DataTrigger>

                                    </Style.Triggers>

                                </Style>
                            </TextBlock.Style>

                        </TextBlock>

                        <TextBlock
                            Margin="0,3,0,0"
                            FontSize="13"
                            Foreground="{StaticResource Mnemora.Brush.TextSecondary}"
                            Text="{Binding SectionName}"
                            TextTrimming="CharacterEllipsis" />

                    </StackPanel>

                    <Button
                        Grid.Column="2"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource Mnemora.DialogCloseButton}">

                        <materialDesign:PackIcon
                            Width="20"
                            Height="20"
                            Foreground="{Binding Foreground,
                                RelativeSource={RelativeSource AncestorType=Button}}"
                            Kind="Close" />

                    </Button>

                </Grid>

                <!-- Описание -->

                <TextBlock
                    Margin="0,24,0,0"
                    FontSize="15"
                    Foreground="{StaticResource Mnemora.Brush.TextSecondary}"
                    LineHeight="23"
                    TextWrapping="Wrap">

                    <TextBlock.Style>
                        <Style TargetType="TextBlock">

                            <Setter
                                Property="Text"
                                Value="Раздел будет удалён без возможности восстановления." />

                            <Style.Triggers>

                                <DataTrigger
                                    Binding="{Binding HasTopics}"
                                    Value="True">

                                    <Setter
                                        Property="Text"
                                        Value="{Binding TopicsMessage}" />

                                </DataTrigger>

                            </Style.Triggers>

                        </Style>
                    </TextBlock.Style>

                </TextBlock>

                <!-- Ошибка команды -->

                <Border
                    Margin="0,18,0,0"
                    Padding="12,9"
                    Background="#FFF1F1"
                    CornerRadius="8">

                    <Border.Style>
                        <Style TargetType="Border">

                            <Setter
                                Property="Visibility"
                                Value="Collapsed" />

                            <Style.Triggers>

                                <DataTrigger
                                    Binding="{Binding HasError}"
                                    Value="True">

                                    <Setter
                                        Property="Visibility"
                                        Value="Visible" />

                                </DataTrigger>

                            </Style.Triggers>

                        </Style>
                    </Border.Style>

                    <Grid>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <materialDesign:PackIcon
                            Width="19"
                            Height="19"
                            VerticalAlignment="Top"
                            Foreground="#C62828"
                            Kind="AlertCircleOutline" />

                        <TextBlock
                            Grid.Column="1"
                            Margin="9,0,0,0"
                            FontSize="13"
                            Foreground="#A52222"
                            Text="{Binding ErrorMessage}"
                            TextWrapping="Wrap" />

                    </Grid>

                </Border>

                <!-- Кнопки -->

                <Grid Margin="0,26,0,0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="14" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--
                    Для непустого раздела эта кнопка занимает всю строку
                    и получает подпись «Понятно».
                    -->

                    <Button
                        Grid.Column="0"
                        Command="{Binding CancelCommand}">

                        <Button.Style>
                            <Style
                                BasedOn="{StaticResource Mnemora.SecondaryButton}"
                                TargetType="Button">

                                <Setter
                                    Property="Content"
                                    Value="Отмена" />

                                <Style.Triggers>

                                    <DataTrigger
                                        Binding="{Binding HasTopics}"
                                        Value="True">

                                        <Setter
                                            Property="Grid.ColumnSpan"
                                            Value="3" />

                                        <Setter
                                            Property="Content"
                                            Value="Понятно" />

                                    </DataTrigger>

                                </Style.Triggers>

                            </Style>
                        </Button.Style>

                    </Button>

                    <!-- Для непустого раздела кнопка удаления скрыта -->

                    <Button
                        Grid.Column="2"
                        Command="{Binding DeleteCommand}"
                        IsDefault="True">

                        <Button.Style>
                            <Style
                                BasedOn="{StaticResource Mnemora.PrimaryButton}"
                                TargetType="Button">

                                <Setter
                                    Property="Background"
                                    Value="#C62828" />

                                <Setter
                                    Property="BorderBrush"
                                    Value="#C62828" />

                                <Setter
                                    Property="Content"
                                    Value="Удалить раздел" />

                                <Style.Triggers>

                                    <DataTrigger
                                        Binding="{Binding HasTopics}"
                                        Value="True">

                                        <Setter
                                            Property="Visibility"
                                            Value="Collapsed" />

                                    </DataTrigger>

                                    <DataTrigger
                                        Binding="{Binding IsDeleting}"
                                        Value="True">

                                        <Setter
                                            Property="Content"
                                            Value="Удаляем..." />

                                    </DataTrigger>

                                </Style.Triggers>

                            </Style>
                        </Button.Style>

                    </Button>

                </Grid>

            </StackPanel>

        </Border>

    </Grid>

</UserControl>