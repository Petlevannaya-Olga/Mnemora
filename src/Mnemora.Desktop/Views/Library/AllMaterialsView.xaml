<UserControl
    x:Class="Mnemora.Desktop.Views.Library.AllMaterialsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{StaticResource Mnemora.Brush.SurfaceMuted}">
        <StackPanel Margin="42">
            <TextBlock
                Text="Все материалы"
                Foreground="{StaticResource Mnemora.Brush.TextPrimary}"
                FontSize="34"
                FontWeight="SemiBold" />

            <TextBlock
                Margin="0,8,0,0"
                Text="Статьи и вопросы из всех разделов и тем"
                Foreground="{StaticResource Mnemora.Brush.TextSecondary}"
                FontSize="16" />

            <Border Height="1"
                    Margin="0,22,0,0"
                    Background="{DynamicResource Mnemora.Brush.Border}" />
        </StackPanel>
    </Grid>
</UserControl>